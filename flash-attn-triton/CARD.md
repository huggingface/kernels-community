# flash-attn-triton
Standalone Triton operations extracted from flash-attention (Dao-AILab/flash-attention).

Provides commonly used kernels like cross-entropy, layer normalization, linear, and rotary embeddings without the overhead of full C++ extension builds required by flash-attention.
