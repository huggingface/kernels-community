[general]
name = "weathernext2-banded-attention"
version = 1
license = "Apache-2.0"
# Pure Triton, so the same source serves both. ROCm matters here: the fp32 attention that PyTorch
# falls back to on CUDA is a cutlass kernel that has no ROCm equivalent.
backends = ["cuda", "rocm"]

[general.hub]
repo-id = "kernels-community/weathernext2-banded-attention"

[torch-noarch]

[kernel]
