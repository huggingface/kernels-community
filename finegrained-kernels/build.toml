[general]
name = "finegrained-kernels"
version = 5
license = "Apache-2.0"
backends = [
    "cuda",
    "rocm",
    "xpu",
]

[general.hub]
repo-id = "kernels-community/finegrained-kernels"

[torch-noarch]

[kernel]
